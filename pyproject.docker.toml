[project]
name = "rag-project-2026"
version = "0.1.0"
description = "RAG dockerisé — dépendances runtime uniquement"
requires-python = ">=3.10"

dependencies = [
    "langchain>=1.0.0",
    "langchain-core>=1.0.0",
    "langchain-mistralai>=1.1.2",
    "langchain-ollama>=1.0.1",
    "chromadb>=1.0.0",
    "sentence-transformers>=3.0.0",
    "streamlit>=1.40.0",
    "python-dotenv>=1.2.2",
]
